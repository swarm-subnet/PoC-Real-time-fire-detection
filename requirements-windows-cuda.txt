# Windows/NVIDIA GPU environment for Python 3.12.
#
# Use this for venv-win on the laptop with NVIDIA driver/CUDA 12.9:
#   python -m pip install -r requirements-windows-cuda.txt
#
# The PyTorch CUDA wheels are not served from normal PyPI, so this file adds
# the PyTorch CUDA 12.9 wheel index and pins the matching torch/torchvision pair.
--extra-index-url https://download.pytorch.org/whl/cu129

djitellopy
opencv-python
onnxruntime
ultralytics
transformers>=4.52.0
pillow
torch==2.8.0+cu129
torchvision==0.23.0+cu129
